{
  "extends": "expo/tsconfig.base",
  "compilerOptions": {
    "strict": true
    // Removing baseUrl and paths
  },
  "include": [
    "**/*.ts",
    "**/*.tsx",
    ".expo/types/**/*.ts",
    "expo-env.d.ts"
  ]
}